// Shared query keys that couple producers and consumers across routes.
export const BOOTSTRAP_NEXT_QUERY_KEY = ["bootstrap-next"] as const;
export const VERSION_STATUS_QUERY_KEY = ["version-status"] as const;
